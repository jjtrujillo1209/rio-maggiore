#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../msgpackr-extract/bin/download-prebuilds.js" "$@"
else
  exec node  "$basedir/../../../msgpackr-extract/bin/download-prebuilds.js" "$@"
fi
# cmd-shim-target=/Users/juanjosetrujillo/Documents/PROYECTOS AI /rio-maggiore/node_modules/.pnpm/msgpackr@1.11.12/node_modules/msgpackr-extract/bin/download-prebuilds.js
